# Agent guidance (JakshWealth UI)

| Task | Location |
|------|----------|
| App architecture | `AGENTS.md` (repo root, synced from here) |
| Add a feature page | `.agents/skills/jw-feature-page/SKILL.md` |

Run `npm install` to sync `agent-guidance/` → `.cursor/` and `.agents/`.
